; 文件上传大小限制（与 Nginx 的 client_max_body_size 保持一致）
upload_max_filesize = 100M
post_max_size = 100M

; 脚本执行时间（上传大文件可能耗时较长）
max_execution_time = 300

; 内存限制（处理图片等操作时需要）
memory_limit = 512M

; 同时上传文件数量限制（WordPress 默认可能一次上传多个）
max_file_uploads = 20